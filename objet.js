var student = {
    name: "nicolas",
    favoritefood: "salad",
    city: "paris"
}

var values = object.values(student)
var reslut = 0

values.foreach((value) => {
    result += value, length
})

console.log(result)